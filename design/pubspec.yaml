name: hanagram_design
description: Hanagram tasarım sistemi — marka, bileşenler, tema, FFI köprüsü.
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  shared_preferences: ^2.2.2
